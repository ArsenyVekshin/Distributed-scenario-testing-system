-- Creates application database for the Coordinator service.
-- Temporal databases (temporal, temporal_visibility) are created
-- automatically by the temporalio/auto-setup container.

CREATE DATABASE coordinator;
